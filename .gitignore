# Secrets — nooit committen (CLAUDE.md §9)
.env
.env.*
!.env.example

# Lab-exports met persoonsgegevens — NOOIT in git (ADR-0001). Bevatten namen/geboortedatum;
# alleen lokaal gebruiken (met toestemming), nooit naar de publieke repo.
docs/*.xml
docs/*.xlsx
docs/*.csv

# Dependencies & build
node_modules
dist
dist-ssr
*.local

# Visuele-check screenshots (lokaal; niet in git) — Slice 3b
.screenshots

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.sw?
